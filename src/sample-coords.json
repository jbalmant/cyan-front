function generateRandom(n) {
    result = []
    for (var i = 0; i < n; i++) {
        var lat = (Math.random()*180-90).toFixed(8)
        var lng = (Math.random()*360-180).toFixed(8)
        result.push({'lat':lat, 'lng':lng})
    }
    
    return result;   
}

function generateUsers() {
    result = []

    for (var i = 0; i < 10; i++) {
        result.push({'name': i, 'coords': generateRandom(5)})
    }

    return result
}


